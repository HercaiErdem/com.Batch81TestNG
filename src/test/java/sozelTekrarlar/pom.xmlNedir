package sozelTekrarlar;

public class PomxmlNedir {

    // Pom.xml Dosyasi(File) Nedir
    /*
   *Pom.xml, Maven projesindeki en önemli dosyadır
*Pom dosyasını kullanmamızın sebebi, bu dosyaya eklediğimiz
 Dependencies’i yönetmek içindir. Pom, ayrıca  artifact id, group id,
 ve version gibi proje bilgilerine de sahiptir.

*Dependencies tag lerinin içine proje için ihtiyaç duyduğumuz dependency
leri Mvnrepository.com adresinden alıp ekleriz.

*dependency leri Pom file ına eklerken <properties> satırının altına
<dependencies> </dependencies> tag’inin içine eklediğimizden emin olalım.

*Pom.xml e , Maven projesindeki en önemli dosyadır dedik peki aralarındaki
fark nedir; Maven, proje otomasyon ve yönetim aracıdır (automation and management TOOL),
pom ise bir xml dosyasıdır(FILE). Pom.xml maven'in bir parçasıdır ve pom
dependencies’i yönetir.

